package page_object_model.pages;

import org.openqa.selenium.WebDriver;

public class SingleStationeryPage extends BasePage{

    public SingleStationeryPage(WebDriver driver) {
        super(driver);
    }
}
