package page_object_model.pages;

import org.openqa.selenium.WebDriver;

public class HomeCompliancePage extends BasePage {

    public HomeCompliancePage(WebDriver driver) {
        super(driver);
    }

    // Any actions you want to perform on the HomeCompliancePage can be added here later
}
